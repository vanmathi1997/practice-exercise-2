public class EvenCheck {
    public boolean checkeven(Integer number){
        if(number%2==0)//condition to check the given number is even
        {
            return true;
        }else
            return false;
    }
}
